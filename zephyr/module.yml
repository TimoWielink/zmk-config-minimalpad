# A fixed name keeps the module's Kconfig and CMake symbols the same in CI and
# in local builds, where the checkout directory is named differently.
name: minimalpad
build:
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
