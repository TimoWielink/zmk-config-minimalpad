manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware

  projects:
    - name: zmk
      remote: zmkfirmware
      # The combined MinimalPad/ZMK Studio USB transport uses ZMK's Studio RPC
      # transport seam. Pin the revision so an upstream internal change cannot
      # silently produce firmware with a broken control channel.
      revision: 9ebbeff0a8b69a42f14aec022cdf16c7a107b9e0
      import: app/west.yml

  self:
    path: config
